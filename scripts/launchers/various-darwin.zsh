#!/usr/bin/env zsh

tmuxnew BrishGarden brishgarden
# bgardenpath=~/code/python/BrishGarden
# tmuxnew BrishGarden "dash -c 'cd $(gq $bgardenpath) && uvicorn garden:app --reload'"

# needs to run by cron # tmux new -d -s books "$NIGHTDIR/zsh/book-checker.zsh"
