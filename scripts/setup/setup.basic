#!/usr/bin/env zsh
local setup_dir="$NIGHTDIR/setup"
cd "$setup_dir"

. "${setup_dir}/ins_brewe"
# . "${setup_dir}/ins_asdf"

ins-all

re source "${setup_dir}/setup_dirs" "${setup_dir}/setup_links" "${setup_dir}/setup_misc" "${setup_dir}/git.config" "${setup_dir}/setup.cargo" "${setup_dir}/setup.linux" "${setup_dir}/setup_macOS.zsh" "${setup_dir}/setup_perl.zsh"
