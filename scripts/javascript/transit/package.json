{
  "name": "transit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Departure boards for a public transit network, as a bun CLI and a browser page.",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "colors:refresh": "bun run scripts/colors-refresh.ts",
    "build": "bun build src/page.ts --target browser --outfile page/app.js && bun build src/route.ts --target browser --outfile page/route.js"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
