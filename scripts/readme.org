#+TITLE: scripts/readme

* Licensing
The code that I have written myself is dual-licensed under the MIT license and the latest GPL (whichever license is more convenient for you), but there is some bit of code I have forked from other FOSS-ish sources, which necessarily follow their parent licensing. (The comments indicate when I have forked some code from somewhere else.)

There are some non-FOSS resources in this repo that I have included only for my own personal, home usage, and which you should not use. These files should be mostly obvious (if it's not mine and you don't know where they are from and what license they are under), but here is a non-exhaustive list:

** =./scripts/external/=

** =./scripts/bin/=

** =/scripts/resources/= unless otherwise specified

* Hammerspoon

Hammerspoon shortcut notes are tracked in =./hammerspoon/docs/hammerspoon.md=.

=~/.hammerspoon/init.lua= points at =./hammerspoon/boot.lua=. Lua files in
=./hammerspoon/auto-load/= are loaded alphabetically.

Hammerspoon modal helpers support single-key bindings with =bind_v2= and
multi-key chords with =bind_v3=. See =./hammerspoon/docs/hammerspoon.md= for
the chord behavior and qView examples.

App-toggle hotkeys can name one app or an ordered list of running-app
fallbacks; they toggle the first running candidate and never launch one. The
Telegram hotkey prefers Purple Telegram, then standard Telegram.

* Zsh plugins

The shared =note= skill drafts concise notes under =~/notes=, uses Terra in
Codex or Sonnet in Claude to find a suitable destination, and waits for approval
of the exact path and draft before saving. See =./docs/note-skill.md=.
Its source is =~/notes/skills/note/SKILL.md= in the private =notes-skills=
repository. The linker discovers both that checkout and the public skills.

Shared agent skills are installed by =agent-skills-link=. Codex uses directory
links under =~/.agents/skills=; =agent-skills-prune-legacy-codex= safely removes
our obsolete =~/.codex/skills= links after migration. Bundled =.system= skills
are preserved. See =./docs/agent-done.md=.

The shared =delegate-weaker= skill (alias =weaker=) routes Fable's mechanical work to
Opus and Astra's to GPT 5.6 Sol or Terra, keeping decisions and review with the
parent. See =./docs/delegate-weaker.md= for scope and activation.

[agfi:hold-acquire] takes a self-expiring hold on any resource --
=repo:~/scripts=, =gpu:0=, =service:hs-reload= -- so one agent can do something
atomic without a parallel session walking into it. Exclusive by default;
=--shared= gives the suppression registry the Hammerspoon auto-reloader uses,
which [agfi:hs-reload-hold] is now built on. A Claude Code =PreToolUse= hook
denies edits and commands touching a held resource. A hold ends when you release
it or when the agent holding it dies; =--ttl= adds a hard deadline for the cases
that want one. The implementation is =golang/night_hold=; see =./docs/holds.md=,
and =./docs/disabled/holds-ttl-extension.md= for the hook-driven TTL extension
that was built and then removed.

[agfi:agent-done] recognizes unsandboxed Codex conversation IDs and recovers
tmux pane ownership through process ancestry, including terminal proxies.
Ambiguous ownership saves the report but refuses automatic termination;
=--dry-run= verifies the target without ending the session.

Selected Zsh directories under =./zshlang/= can be structured as public plugins while remaining the local source of truth. Plugin entrypoint files named =*.plugin.zsh= should stay thin loaders; implementation should live in modular =*.zsh= files.

See =./docs/zsh_plugins.md= for layout conventions and generic plugin-manager notes.

The shared =zsh-plugin-authoring= skill in
=./configFiles/agent-skills/zsh-plugin-authoring/SKILL.md= guides plugin extraction
and standalone verification.

The =agent-session= plugin shares exact-conversation resume and tmux identity
with tmux-subagents. See =./zshlang/plugins/agent-session/README.md= for its
standalone API and subdirectory installation.

The =tmux-z= plugin holds the tmux session primitives, the =tmuxnewsh2=
launchers, and =tmuxnewsh2-attach-z=, which opens a session in whatever
directory =z= finds: =codex-t scripts --resume= is =z scripts && codex-m
--resume=. It is a plugin because =./setup/minimal_proxy/.shared.sh= used to
carry a second, drifted copy of those helpers. See
=./zshlang/plugins/tmux-z/README.md= and =./docs/tmux-z-launchers.md=.

=./zshlang/basic/conditions.zsh= keeps only the general predicates, so the
public =basic.plugin.zsh= can expose them; host identity and personal
infrastructure live in =./zshlang/basic/conditions-personal.zsh=, which only
=basic-full.zsh= loads.

Kitty's =cmd+shift+o= resolves attached tmux clients by PID, including numeric
session targets and clients that have switched sessions. Ambiguous live
transcripts open the picker. See =./docs/agent-sessions.md= for lookup order
and regression checks. Codex identity hooks resolve missing transcript paths
by session ID and keep the latest local outcome per pane; see
=./docs/tmux-session-rename.md= for diagnostics.

* Pandoc conversion

[[agfi:pandoc-convert]] (=./zshlang/auto-load/others/pandoc.zsh=) is the single
gateway behind =md2org=, =org2md=, =html2org=, etc. Output fixups live as Lua
filters in =./python/pandoc_filters/= rather than as textual postprocessing, so
they cannot corrupt code/example blocks.

Known conversion problems and their filters are documented per-issue:

- =./docs/md2org-latex/readme.md= — LLM-style =\[...\]= math survives org's parser.
- =./docs/md2org-details.md= — =<details>=/=<summary>= folding, and pandoc's
  obsolete =#+begin_html= raw-HTML wrapper.
- =./docs/html2org-empty-underline.md= — stray =__= from Outlook emails.

=./docs/md-strip-german-lessons.md= covers the two functions that remove the
=Learning German ^_^= sections my LLM instructions append: the =md2org=-path
one for single answers, and the standalone one for conversation exports.

* arXiv downloads

[[agfi:arxiv-dl]] fetches PDFs from =export.arxiv.org= first and falls back to
the =arxiv.org= mirror, because export currently answers 406 on a cache miss and
truncates the PDFs it does serve.

The general half is [[agfi:curl-2dest]] (aliased as =aa-2dest=): download to a
destination, trying a list of URLs in turn, via a =.part= file so a failure
leaves the old file alone. The arXiv half is [[agfi:arxiv-url-alternatives]],
which appends the =arxiv.org= twin of an =export.arxiv.org= URL and is a no-op
for everything else, so callers need no =is this arXiv?= test.

=./docs/arxiv-export-pdf.md= has the measurements, and the two curl traps behind
the same exit code 22: a default =--continue-at -= getting a 416 on an already
complete file, and =retry= being unlimited where a bounded =retry-limited= is
wanted.

* Codex

=codex_status.py= notes are tracked in =./docs/codex_status.md=, including the
=codex-status-notify= / =codex-status-continue-fz= notifiers that wait for the
first auth's rate limit to reset and queue =Continue.= into the threads you
picked, and how usage windows are read now that how many of them a plan reports
varies.

=./docs/codex_loop.md= covers =codex_loop.py=, which runs a prompt repeatedly
and sleeps out the rate limit between runs, sharing that reading.

* Antigravity

=./docs/agy_status.md= covers =agy-status=, which reads the per-model-group
quota that =agy -p /usage= prints without spending any of it, and the
=agy-status-notify= / =agy-status-continue-*= notifiers that wait for the
earliest exhausted group to reset and type =Continue.= into the Antigravity
windows or panes you picked.

* Agent usage limits

=./docs/agent-usage-armed.md= covers the one-shot job shared by the Claude
Code, Codex and Antigravity notifiers: it waits in a tmux session for a reset
time and then either notifies or types =Continue.= into kitty windows, tmux
panes or a Codex queue chosen at arm time, unless you are at the keyboard.
Where each agent's reset time comes from, and the =cck= / =cct= / =ccfront=
entry points, are in =./docs/claude_code_usage.md=, =./docs/codex_status.md=
and =./docs/agy_status.md=; =agent-usage-continue-at= names the time by hand
for any agent, when there is no reset source or you do not trust the one there
is.
=agent-status= (=agst=) prints all three usage reports at once, in parallel.

=./docs/agent-auto-continue.md= covers =/auto-continue=, the skill shared by
all three agents that lets a session arm itself ahead of time: a watcher per
account polls the usage and, once it is blocked, arms the same job with the
registered sessions as targets, so they resume at the reset with nothing to
pick.

* Global npm installs

pnpm cannot install any package whose tarball is larger than roughly 50MB: it
hashes the tarball in a worker thread, where the structured clone has already
downgraded the =Buffer= to a plain =Uint8Array=, and Node stringifies those.
Both codex and claude-code are over that line, so [agfi:codex-install] and
[agfi:claude-install] go through npm. See =./docs/npm-global-installs.md=,
which also covers the orphaned npm staging directory that silently broke
reinstalls for eight months.

* Tailscale

=setup/setup_tailscale.zsh= installs and enables Tailscale without storing
tailnet, account, hostname, or auth-key material in this public repo. See
=./docs/tailscale-setup.md=.

The same document covers Android DNS diagnostics: compare normal HTTPS with a
hostname-preserving DNS bypass, then test public resolvers and Tailscale's local
Quad100 resolver separately.

* Emacs terminal colors

=setup/terminfo-24bit.src= provides one truecolor definition for Kitty and
Termux. Installation and verification: =./docs/emacs-truecolor.md=.
=emc-mobile= opens a truecolor terminal frame with mobile line-number suppression.
Copies from these frames use Emacs's native OSC 52 support to reach the terminal
client's clipboard; limits and tmux setup are in =./docs/emacs-mobile-clipboard.md=.
=emc-tealy= adds an authenticated SSH fallback for copies larger than the OSC 52
limit, caching phone reachability for 15 minutes; =emc-tealy-cache-clear= expires
that cache explicitly.
=emc-tealy= also fetches fresh clipboard text over SSH on normal Emacs paste,
with a bounded, cancellable wait; desktop and plain mobile paste are unchanged.
=M-x night/ssh-paste-toggle= toggles automatic reads; =night/ssh-paste= and
=night/tealy-paste= provide explicit reads even when automatic reads are off.
=tealy-paste= prints the phone clipboard over SSH; the generic helper is
=h-paste-from-remote-termux HOST=. See =./docs/remote-termux-clipboard.md=.
Termux does not answer OSC 52 clipboard queries; its Paste action works through
the existing terminal connection without a reverse SSH connection.
The deferred receiver design is in =./.docs/plans/clipboard-listener.md=.

=tealy-alarm TIMESPEC [MESSAGE]= sets an alarm in the phone's own clock app over
SSH, so it rings through Doze and silent mode and survives Termux being killed;
the generic helper is =h-termux-alarm-set HOST=. =tealy-timer=, =tealy-notify=
and =tealy-toast= round out the set. Times are parsed here by =datenat-unix-v2=
(compact durations like =1h30m= plus chrono prose) and cross the wire as a unix
epoch, which the phone renders against Android's system timezone. The resolved
time is printed before the intent is sent, and =termux_alarm_dryrun=y= stops
there, which matters because alarms cannot be removed programmatically. An alarm that silently never
appears means the =SET_ALARM= intent hit an invisible app-chooser dialog; that
failure reports success from every angle, and is explained along with the
unsupported operations in =./docs/remote-termux-alarms.md=.

=emc-mobile-tmux= hosts that frame in a tmux session named =emacs-mobile=, so its
tty outlives the connection instead of dying with it, and reattaches to it later.
=emc-tealy-tmux= does the same in a separate =emacs-tealy= session.

Under tmux the colors are re-encoded through the attached client's terminfo, so a
Termux client reporting =xterm-kitty= received a spelling it cannot parse and
printed the escapes as text. Cause and fix: =./docs/tmux-termux-truecolor.md=.

* Terminal titles

[agfi:tty-title] labels a kitty tab with what it is running. Inside tmux it
reached only the pane title: =set-titles= is off by default, so tmux never
forwarded it to the attached client. Cause and fix, along with a command
injection found in the same function: =./docs/tmux-tty-title.md=.

[agfi:codex-m] shows =project | task name= in the terminal title without the
working spinner, reducing Termux toasts while keeping animations inside Codex.
Thread-name generation can still briefly animate the title.
Explicit =-c tui.terminal_title= arguments override that launcher default; the
tradeoffs and Termux behavior are documented in =./docs/tmux-tty-title.md=.

[agfi:codex-long] calls [agfi:codex-m] with the model-neutral =long= profile.
It regenerates =$CODEX_HOME/long.config.toml= (default
=~/.codex/long.config.toml=) on every launch, keeping the profile's source of
truth in the function.  The profile makes the Codex client budget for an
872,000-token context and compact at 828,400 tokens; pass =--model= when a
model other than the normal default is wanted.  The server still enforces its
own context limit.

* UNIX sockets

kitty's remote control, the Emacs server and the iTerm focus helper all bind a
socket under =$NIGHT_SOCKETS_DIR=, =~/.local/state= by default, and
emphatically not under =~/tmp=: that directory gets swept, and a socket deleted
from under a live listener cannot be restored at all. See
=./docs/unix-sockets.md=, which also covers why kitty in particular can then
only be fixed by restarting it.

* ntag

ntag notes are tracked in =./docs/ntag/=.

* Bells

=bell-auto= escalates from sound to a desktop notification to Telegram, and picks
its behaviour from whether we are at the office. Claude and Codex notifications
include the thread name when available, e.g. =Claude [scripts · Fix terminal titles]:
needs your permission= or =Codex awaits! [scripts · Fix terminal titles]=. Lookup
uses only the supplied Claude transcript or the Codex ID in the current
=CODEX_HOME= index, with a two-second limit and the original format on failure.
Names are limited to one line and 120 characters; acknowledgement groups stay
keyed by session ID. Codex realtime voice-chat tasks are suppressed before any
sound, desktop, or Telegram transport, using their stable =thread_source= metadata
rather than their mutable title. Run =brishz-restart= after updating the hooks;
queued text stays as captured. See =./docs/bell-auto.md= for lookup, suppression,
and fallback details.

* CIS thesis applications

=cis-applications2org= converts the Schütze Lab (CIS) thesis/internship
application form export into a filtered org-mode tree. It lives in its own repo
rather than here, because it handles applicants' personal data and needs a
=.gitignore= that refuses the exports outright:
[[zf:~\[cod\]/python/cis-applications2org/][~\[cod\]/python/cis-applications2org/]].

#+begin_src zsh :eval never
uv tool install 'git+https://github.com/NightMachinery/cis-applications2org.git'
#+end_src

* Audio leak guard

=audio-guard= mutes the output device when audio would be audible to colleagues
at the office and nobody is at the desk, and unmutes when you unlock. Installed
as a LaunchAgent from =./launchers/audio-guard/=. See =./docs/audio-guard.md=.

* External display brightness and contrast

=brightness-get= / =-set= / =-inc= / =-dec= drive external monitors over DDC/CI,
not just the built-in panel, because IOKit cannot reach an external one at all.
Each display is routed to whichever backend can talk to it —
[[agfi:brightness-displays]] shows which — and a selector picks the display when
several are attached, defaulting to the main one.

=contrast-get= / =-set= / =-inc= / =-dec= are the same four over the same
selectors, DDC-only since IOKit exposes no contrast. hyper+F1/F2 steps
brightness and hyper+ctrl+F1/F2 steps contrast, both coalescing their presses so
a key hold cannot outrun the DDC bus.

=display-black-on= / =-off= / =-toggle= blank a display outright. That is not the
same as brightness 0: on a built-in panel IOKit really does cut the backlight,
but DDC luminance 0 leaves an external one visibly lit, so those get a zero gamma
table with the DDC levels floored underneath. hyper+shift+F1/F2 go through here.
F1 also locks the keyboard and mouse for as long as the screen is black, so a
brushed key cannot type into an unseen window or turn the brightness back up;
hyper+shift+F2 releases both, and so does [[agfi:display-black-off]], which
every unblack path runs through.

The levels are remembered per display and put back per display. Restoring
re-resolves each row by CGDirectDisplayID rather than by the positional
backend-local id it was saved with, so a monitor unplugged or docked mid-blackout
cannot have another one's level written to it; a display that is gone at that
moment has its levels parked until it is back. A level that was lost rather than
read lands on a configured fallback, because a row exists only where something
was floored, and leaving it alone leaves the panel dark.

See =./docs/external-display-brightness.md=, which also records how often DDC
reads come back corrupt and what is done about it.

* Fill-in-the-middle completion

=alt+.= completes the line you are halfway through writing, sending the text
left of the cursor as the prefix and the text right of it as the suffix, and
inserting one line at the cursor. Codestral answers in about 0.3s;
[[agfi:fim-provider-select]] switches to DeepSeek when the output matters more
than the wait.

[[agfi:fim-get]] is the request on its own, in =zshlang/auto-load/others/fim.zsh=,
so it also works in a pipe or over brish. Only the widget and the bindings are
interactive-only, in =zshlang/interactive/auto-load/FIM.zsh=. The Emacs twin is
=night/fim-get= in =~[dom]/autoload/night-mistral-fim.el=; the two speak the same
body to the same providers and want changing together.

Inserting refetches the autosuggestion. Our widgets are named =zle-*= so
zsh-autosuggestions never sees the edit, and the suggestion computed for the
old line both paints the freshly inserted text in the suggestion colour and
stays acceptable: with =ec hello= suggesting =' world and then some'=,
completing to =ec hello world= and pressing right arrow gave =ec hello world
world and then some=.

The prefix is =${PREBUFFER}${LBUFFER}=, so a completion asked for inside a
continuation -- an unclosed quote, a =for= still waiting for its =done= -- sees
the lines you already typed rather than just the one you are on. The completion
itself is still capped at a line.

Status goes to =zle -M=, after a blank line so it cannot be read as part of the
command, and is told apart by symbols rather than colour: =❄= marks the line as
ours, then =⋯= in flight, =✓= inserted, =∅= nothing but nothing wrong, =✗=
failed. Not a colour because =zle -M= cannot carry one -- it visualises control
characters instead of emitting them -- and because the slot that *can*,
=POSTDISPLAY=, belongs to zsh-autosuggestions: right arrow and twelve other
widgets splice whatever sits there into the command line as real text.

Outside a terminal the same completion is on =hyper+shift+right= (and
=hyper+ctrl+right= for DeepSeek), from =hammerspoon/core/fim.lua=. It reads the
text around the cursor through the Accessibility API where the app exposes it,
and otherwise through a =shift+cmd+up= / =cmd+c= / =shift+cmd+down= / =cmd+c=
dance over the clipboard -- Purple Telegram's Qt draft box does not appear in
the accessibility tree at all, and kitty's is always empty. The completion
arrives as a ghost in an alert band: any key inserts it, =Esc= discards it,
=⇧Esc= puts it on the clipboard, and anything we could not paste lands there
too. Hyper and purple keys pass through the ghost untouched.

In kitty and Emacs the same chord posts =alt+.= instead, since both already have
a FIM of their own that knows the buffer better than the Accessibility API can.
=fimAppPolicy= holds that, keyed by bundle ID.

See =./docs/fim.md=, which records the things that cost real time (ZLE's
=BUFFER= and =CURSOR= are not bound inside a =zle -F= handler; cancelling a
request by process group takes the request that replaces it down too; and
=cat -v= renders a real ESC byte and a literal =^[= pair identically, so it
cannot be used to check any of this), and why =fim_strip_space_p= is off: the
leading space these models return is almost always the right one.
